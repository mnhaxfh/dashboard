# ─────────────────────────────────────────────────────────────
# DB TRUNG GIAN (Supabase project riêng cho dashboard)
# ─────────────────────────────────────────────────────────────
# Backend (FastAPI + SQLAlchemy) chỉ cần biến này.
# Lấy tại: Supabase Dashboard → Settings → Database → URI
DATABASE_URL=

# ─────────────────────────────────────────────────────────────
# HIS (Supabase cũ — nguồn dữ liệu, dùng bởi sync_from_his.py)
# ─────────────────────────────────────────────────────────────
HIS_SUPABASE_URL=
HIS_SUPABASE_KEY=

# Alias cho simulate_demo.py (dùng tên cũ SUPABASE_URL / SUPABASE_SECRET_KEY)
SUPABASE_URL=
SUPABASE_SECRET_KEY=
